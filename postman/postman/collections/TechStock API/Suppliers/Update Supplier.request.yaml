$kind: http-request
name: Update Supplier
method: PATCH
order: 6000
url: '{{baseUrl}}/suppliers/{{supplierId}}'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "phone": "0700000009"
    }
