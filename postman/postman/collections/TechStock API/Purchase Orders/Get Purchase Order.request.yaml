$kind: http-request
name: Get Purchase Order
method: GET
order: 3000
url: '{{baseUrl}}/purchase-orders/{{purchaseOrderId}}'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
